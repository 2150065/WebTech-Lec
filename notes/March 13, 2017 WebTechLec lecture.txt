March 13, 2017
CSS Rule
-by origin and importance
	*User agent important declaration
	*user important declaration
	*Author important declaration
	*User normal declaration
	*User agent normal declaration
-by specificity
	*Inline style
	*number of ID selector
	*number of class selectors attribute selectors and pseudo-classes
	*Number of typeselectors and pseudo elements
-by order
	*

CSS Declaration
	-Properties
		*short-hand properties
		*vendor-specific extension
	-Values
		*keywords
		*numbers (integer, and real in decimal position)
		*Dimension
			=lenght,angle, duration frequency, resolution
				+lenght units:
					>font-relative: em, ex, ch, rem
					>view port percentage: vw, vh, vmin, vmax
					>absolute lengths: cm, mm, q, inch, pt, pc, px
			=Angle units: deg, grad, rad 
			=duration units: s, ms
			=frequency units: hz, khz
			=Resolution units: dpi, dpcm, dppx
	-Percentages
	-URLs and URIs
	-Colors
	-Strings
	-functions: calc(), attr(), counter(), counters(), linear-gradient(), radial-gradient()
		translate(), scale(), rotate() etc....